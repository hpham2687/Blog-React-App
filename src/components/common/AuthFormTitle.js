import { Form } from "@ahaui/react";
import styled from "styled-components";

export const AuthFormTitle = styled(Form.Label)`
  text-transform: uppercase;
  font-weight: 400;
  font-size: 1.2rem;
  margin-bottom: 0;
  border-bottom: 1px solid var(--colorInformation);
  width: fit-content;
  margin: 0 auto;
`;
